name: Supabase Migration
on: workflow_dispatch
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Run schema migration
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          python3 << 'EOF'
          import json, os, urllib.request

          url = os.environ['SUPABASE_URL']
          key = os.environ['SUPABASE_KEY']

          sql = """
          -- Voice conversation logging
          CREATE TABLE IF NOT EXISTS voice_conversations (
            id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
            call_sid TEXT NOT NULL,
            from_number TEXT,
            department TEXT CHECK (department IN ('sales','support','general')),
            turn INTEGER DEFAULT 0,
            caller_text TEXT,
            ai_response TEXT,
            created_at TIMESTAMPTZ DEFAULT NOW()
          );
          CREATE INDEX IF NOT EXISTS idx_voice_conv_call ON voice_conversations(call_sid);
          CREATE INDEX IF NOT EXISTS idx_voice_conv_dept ON voice_conversations(department);

          -- Client users
          CREATE TABLE IF NOT EXISTS client_users (
            id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
            email TEXT UNIQUE NOT NULL,
            password_hash TEXT NOT NULL,
            name TEXT NOT NULL,
            company TEXT,
            plan TEXT DEFAULT 'starter',
            role TEXT DEFAULT 'client',
            active BOOLEAN DEFAULT true,
            stripe_customer_id TEXT,
            created_at TIMESTAMPTZ DEFAULT NOW(),
            last_login TIMESTAMPTZ
          );
          CREATE INDEX IF NOT EXISTS idx_client_users_email ON client_users(email);

          -- Client content
          CREATE TABLE IF NOT EXISTS client_content (
            id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
            client_id UUID REFERENCES client_users(id) ON DELETE CASCADE,
            type TEXT CHECK (type IN ('blog','social','newsletter','image','video','ad')),
            title TEXT NOT NULL,
            status TEXT DEFAULT 'draft',
            platform TEXT,
            word_count INTEGER,
            seo_score INTEGER,
            published_at TIMESTAMPTZ,
            created_at TIMESTAMPTZ DEFAULT NOW()
          );
          CREATE INDEX IF NOT EXISTS idx_client_content_client ON client_content(client_id);

          -- Client analytics
          CREATE TABLE IF NOT EXISTS client_analytics (
            id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
            client_id UUID REFERENCES client_users(id) ON DELETE CASCADE,
            date DATE NOT NULL,
            page_views INTEGER DEFAULT 0,
            unique_visitors INTEGER DEFAULT 0,
            leads_generated INTEGER DEFAULT 0,
            social_engagement INTEGER DEFAULT 0,
            voice_calls INTEGER DEFAULT 0,
            created_at TIMESTAMPTZ DEFAULT NOW(),
            UNIQUE(client_id, date)
          );
          CREATE INDEX IF NOT EXISTS idx_client_analytics ON client_analytics(client_id, date);

          -- Client social posts
          CREATE TABLE IF NOT EXISTS client_social_posts (
            id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
            client_id UUID REFERENCES client_users(id) ON DELETE CASCADE,
            platform TEXT NOT NULL,
            content TEXT,
            status TEXT DEFAULT 'scheduled',
            likes INTEGER DEFAULT 0,
            comments INTEGER DEFAULT 0,
            shares INTEGER DEFAULT 0,
            impressions INTEGER DEFAULT 0,
            published_at TIMESTAMPTZ,
            created_at TIMESTAMPTZ DEFAULT NOW()
          );
          CREATE INDEX IF NOT EXISTS idx_client_social ON client_social_posts(client_id);

          -- Enable RLS
          ALTER TABLE voice_conversations ENABLE ROW LEVEL SECURITY;
          ALTER TABLE client_users ENABLE ROW LEVEL SECURITY;
          ALTER TABLE client_content ENABLE ROW LEVEL SECURITY;
          ALTER TABLE client_analytics ENABLE ROW LEVEL SECURITY;
          ALTER TABLE client_social_posts ENABLE ROW LEVEL SECURITY;

          -- Service role policies
          DO $$ BEGIN
            CREATE POLICY service_all_voice ON voice_conversations FOR ALL USING (true);
          EXCEPTION WHEN duplicate_object THEN NULL; END $$;
          DO $$ BEGIN
            CREATE POLICY service_all_users ON client_users FOR ALL USING (true);
          EXCEPTION WHEN duplicate_object THEN NULL; END $$;
          DO $$ BEGIN
            CREATE POLICY service_all_content ON client_content FOR ALL USING (true);
          EXCEPTION WHEN duplicate_object THEN NULL; END $$;
          DO $$ BEGIN
            CREATE POLICY service_all_analytics ON client_analytics FOR ALL USING (true);
          EXCEPTION WHEN duplicate_object THEN NULL; END $$;
          DO $$ BEGIN
            CREATE POLICY service_all_social ON client_social_posts FOR ALL USING (true);
          EXCEPTION WHEN duplicate_object THEN NULL; END $$;
          """

          # Execute via Supabase REST RPC
          req = urllib.request.Request(
            f'{url}/rest/v1/rpc/exec_sql',
            data=json.dumps({'query': sql}).encode(),
            headers={
              'apikey': key,
              'Authorization': f'Bearer {key}',
              'Content-Type': 'application/json'
            },
            method='POST'
          )
          try:
            resp = urllib.request.urlopen(req)
            print(f'Migration success: {resp.status}')
          except urllib.error.HTTPError as e:
            body = e.read().decode()
            print(f'RPC exec_sql not available ({e.code}), trying direct SQL via pg...')
            # Fallback: try individual table checks via REST API
            tables = ['voice_conversations', 'client_users', 'client_content', 'client_analytics', 'client_social_posts']
            for table in tables:
              try:
                check = urllib.request.Request(
                  f'{url}/rest/v1/{table}?limit=0',
                  headers={'apikey': key, 'Authorization': f'Bearer {key}'}
                )
                urllib.request.urlopen(check)
                print(f'  {table}: EXISTS')
              except urllib.error.HTTPError as e2:
                if e2.code == 404:
                  print(f'  {table}: MISSING - needs manual creation')
                else:
                  print(f'  {table}: ERROR {e2.code}')
          EOF
