# DISABLED — Using Netlify instead
# Original: deploy_cloudflare.yml
# To re-enable: remove this file and use netlify deploy workflow
name: (disabled) Cloudflare Deploy
on:
  workflow_dispatch:
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Using Netlify instead"
