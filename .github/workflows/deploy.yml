name: Deploy Site to Netlify
on:
  workflow_dispatch:  # DISABLED FROM SCHEDULE — netlify bandwidth exceeded
  # push:  # disabled
  #   branches: [main]
  #   paths: ["site/**"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy paused
        run: echo "Netlify deploy paused — bandwidth limit exceeded. Use Cloudflare Pages instead."
